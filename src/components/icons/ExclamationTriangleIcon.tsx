import React from 'react';
import { BsExclamationTriangle } from 'react-icons/bs';

export const ExclamationTriangleIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsExclamationTriangle className={className} />
);
